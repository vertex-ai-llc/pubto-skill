interface:
  display_name: "Pubto Publish"
  short_description: "Publish and manage local services through Pubto"
  default_prompt: "Use $pubto-publish to publish or map a local service through Pubto and verify the result."
